/*
The following code keeps looping forever. Why is that?
Because there is no stopping condition in the for statement
Also modify it so that it stops after the first iteration.
*/

let i;

for (i = 0; i < 3; i += 1) {
  console.log("and on");
}